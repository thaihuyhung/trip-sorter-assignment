import { TransportIconPipe } from './transport-icon.pipe';

describe('TransportIconPipe', () => {
  it('create an instance', () => {
    const pipe = new TransportIconPipe();
    expect(pipe).toBeTruthy();
  });
});
