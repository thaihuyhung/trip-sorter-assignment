import { DealPricePipe } from './deal-price.pipe';

describe('DealPricePipe', () => {
  it('create an instance', () => {
    const pipe = new DealPricePipe();
    expect(pipe).toBeTruthy();
  });
});
